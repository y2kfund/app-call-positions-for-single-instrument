import { default as callPositions } from './views/Positions.vue';
export { callPositions };
export default callPositions;
export interface callPositionsProps {
    symbolRoot: string;
    userId?: string | null;
}
